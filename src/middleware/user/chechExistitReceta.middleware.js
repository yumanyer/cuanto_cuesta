import { dataBase } from "../../config/connectDB.config.js";


export const checkExistitReceta = async (req, res, next) => {
    try {
        const { id } = req.body;
        const user_id = req.user.id;
        
        
    } catch (error) {
        
    }
};